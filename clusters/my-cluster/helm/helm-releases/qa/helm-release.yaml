apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: hello-world-qa
  namespace: morgan-qa
spec:
  interval: 5m
  chart:
    spec:
      chart: ../../../../helm-charts/hello-world
      sourceRef:
        kind: GitRepository
        name: my-flux-repo
  values:
    replicaCount: 1
